#include <stdio.h>

/**
 * main - This is where my program starts from
 *
 * Return: 0 if program runs without error
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
